---
name: doc-manager
description: Alias wrapper for docs-manager. Use when the user explicitly types /doc-manager by habit and intends to run the repository wiki/docs workflow.
---

# Doc Manager

This is an alias for `docs-manager`.

## Procedure

1. Read `AGENTS.md`.
2. Use the `docs-manager` workflow exactly.
3. Treat this skill as a convenience alias for users who type `/doc-manager` instead of `/docs-manager`.

## Related

- `docs-manager`

